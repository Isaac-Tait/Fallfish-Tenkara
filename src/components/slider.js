import React from "react"

import { image, StaticQuery } from "gatsby"

const Slider = () => {
    <StaticQuery 
        query={graphql`
            query {
                
            }
    }
} 

export default Slider